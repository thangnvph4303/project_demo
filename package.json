{
  "name": "asm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "vite": "^4.1.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "build": "^0.1.4",
    "tailwind": "^4.0.0"
  }
}
